<div class="row collapse">
    <div class="columns">
        <h5 for="url">Zadejte url adresu výpisu, který chcete sledovat &nbsp;-&nbsp;
            <a href="#" {{action 'showHelp'}}><b>nápověda</b></a></h5>
    </div>
</div>
{{#each message in errors}}
<div class="row">
    <div class="columns">
        <div data-alert class="alert-box warning">
            {{ message }}
        </div>
    </div>
</div>
{{/each}}
<div class="row collapse">
    <div class="small-1 columns">
        <span class="prefix">Url</span>
    </div>
    <div class="small-8 columns">
        {{input value=url id="url" placeholder="http://www.sreality.cz/hledani/prodej/domy?price_max=3000000"
        focus-out="processUrl" insert-newline="processUrl" classBinding="showSummary:disabled"}}
    </div>
    <div class="small-3 columns">
        {{#if showSummary}}
        <button
        {{action 'processUrl'}} class="button postfix">Změnit</button>
        {{else}}
        <button
        {{action 'processUrl'}} class="button postfix">Odeslat</button>
        {{/if}}
    </div>
</div>

{{#if showSummary}}
<div class="row">
    <div class="columns">
        <div data-alert class="alert-box">
            Na <a {{ bind-attr href=url }} target="_blank">zadané adrese</a> jsme nalezli
            <strong>{{ adsCount }} inzerátů</strong>.<br/>
            {{#if showPayedWarning}}
            <strong><br/>
                Do {{GLOBAL_FREE_LIMIT}} inzerátů je naše služba zcela zdarma, pokud chcete sledovat více inzerátů,
                je třeba si u nás objednat placený tarif. Více viz {{#link-to 'pricelist' }}Ceník{{/link-to}}.<br/>
            </strong><br/>
            {{/if}}
            Pokud chcete tyto inzeráty sledovat, zadejte Váš e-mail a odešlete.<br/>
            Od toho okamžiku Vám budou chodit upozornění na
            <strong>nové</strong> a <strong>změněné</strong> inzeráty.
        </div>
    </div>
</div>

<div class="row collapse">
    <div class="small-1 columns">
        <span class="prefix">Email</span>
    </div>
    <div class="small-8 columns">
        {{input value=email id="email" placeholder="vaše e-mailová adresa, např. pepa.vomacka@seznam.cz"
        insert-newline="submit"}}
    </div>
    <div class="small-3 columns">
        <button
        {{action 'submit'}} class="button postfix">Začít sledovat</button>
    </div>
</div>
<div class="row collapse">
    <div class="columns small-offset-1">
        {{input type="checkbox" id="termsAgreement" checked=termsAgreement}}
        <label for="termsAgreement">souhlasím se
            <a {{action 'showTerms'}}>smluvními podmínkami služby</a></label>
    </div>
</div>
{{/if}}

<div id="help" class="row panel" style="display:none;">
    <div class="columns">
        <a class="close-reveal-modal right" {{action 'showHelp'}}>&#215;</a>
        {{partial 'help'}}
    </div>
</div>

<div id="terms" class="reveal-modal" data-reveal>
    <div class="panel">
        {{partial 'terms'}}
        <div class="text-center">
            <a {{action 'hideTerms'}} class="button">Ok, chápu</a>
        </div>
    </div>
    <a {{action 'hideTerms'}} class="close-reveal-modal">&#215;</a>
</div>
